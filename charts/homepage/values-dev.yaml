replicaCount: 1

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 256Mi

ingress:
  enabled: false

autoscaling:
  enabled: false # pasbesoin en dev, on reste a 1 pod fixe, attention à modif pr 

dashboard:
  settings:
    title: "Homepage Dashboard [DEV]"
    headerStyle: clean
